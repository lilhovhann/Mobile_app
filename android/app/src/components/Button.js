const Button = (props) => {
    return (
        <>
            <TouchableOpacity  style={{ backgroundColor: props.color}}>
            this is button
            </TouchableOpacity>
        </>
    )
}

export default Button;
